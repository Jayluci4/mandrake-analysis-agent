# Production Environment Configuration for Biomni Backend

# Application Settings
NODE_ENV=production
HOST=0.0.0.0
PORT=8000

# Data Storage Paths
BIOMNI_DATA_PATH=/app/data
DATABASE_PATH=/app/storage/conversations.db
TEMP_PATH=/app/temp
UPLOADS_PATH=/app/uploads

# Authentication - Research Team Credentials
# Change these passwords in production!
RESEARCHER1_PASSWORD=biolab2024
RESEARCHER2_PASSWORD=genomics2024
ADMIN_PASSWORD=biomni2024
GUEST_PASSWORD=demo2024

# Security Settings
# Maximum concurrent sessions per user (auto-recycles oldest sessions when limit reached)
MAX_SESSIONS_PER_USER=10
SESSION_TIMEOUT_MINUTES=30

# AWS/S3 Configuration (if using cloud storage)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
S3_BUCKET_NAME=your_s3_bucket

# Logging
LOG_LEVEL=INFO
LOG_FILE_PATH=/app/logs/biomni.log

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com